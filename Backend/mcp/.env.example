MCP_HOST=0.0.0.0
MCP_PORT=8081

# 与后端容器加入同一个 Docker 网络后，通过容器名访问
PARA_BASE_URL=http://parabbs-backend:8080/bbs

# 逗号分隔，不包含协议和路径
MCP_ALLOWED_HOSTS=localhost,127.0.0.1,mcp.example.com

PARA_REQUEST_TIMEOUT_MS=10000
PARA_TOKEN_CACHE_SECONDS=30