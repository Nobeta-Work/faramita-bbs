<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.nobeta.bbs.module.auth.mapper.AuthMapper">



    <!-- selectAuthUserByUsername --> 

    <select id="selectAuthUserByUsername" parameterType="string"
            resultType="cn.nobeta.bbs.module.user.entity.User">
        SELECT
            id, username, password, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- selectRoleCodesByUserId --> 

    <select id="selectRoleCodesByUserId" parameterType="long"
            resultType="string">
        SELECT r.role_code
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>



    <!-- selectPermCodesByUserId --> 

    <select id="selectPermCodesByUserId" parameterType="long"
            resultType="string">
        SELECT DISTINCT p.perm_code
        FROM sys_user_role ur
        INNER JOIN sys_role_perm rp ON rp.role_id = ur.role_id
        INNER JOIN sys_perm p ON p.id = rp.perm_id
        WHERE ur.user_id = #{userId}
    </select>



    <!-- insertUser --> 

    <insert id="insertUser" parameterType="cn.nobeta.bbs.module.user.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            username,
            password,
            nickname,
            sex,
            race,
            status
        ) VALUES (
            #{username},
            #{password},
            #{nickname},
            #{sex},
            #{race},
            #{status}
        )
    </insert>

    <!-- insertUserDefaultRole --> 

    <insert id="insertUserDefaultRole" parameterType="long">
        INSERT INTO sys_user_role (
            user_id,
            role_id
        )
        SELECT
            #{userId},
            r.id
        FROM sys_role r
        WHERE r.role_code = 'USER'
    </insert>
</mapper>