<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.nobeta.bbs.module.like.mapper.LikeMapper">


    <!-- selectLikerIdsByBlogId --> 

    <select id="selectLikerIdsByBlogId"
            resultType="long">
        SELECT user_id
        FROM like_blog
        WHERE blog_id = #{blogId}
    </select>

    <select id="selectLikerIdsByCommentId"
            resultType="long">
        SELECT user_id
        FROM like_comment
        WHERE comment_id = #{commentId}
    </select>

    <insert id="insertBlogLike">
        INSERT IGNORE INTO like_blog (
            blog_id,
            user_id,
            create_time
        ) VALUES (
            #{blogId},
            #{userId},
            #{createTime}
        )
    </insert>

    <delete id="deleteBlogLike">
        DELETE FROM like_blog
        WHERE blog_id = #{blogId}
          AND user_id = #{userId}
    </delete>

    <update id="refreshBlogLikeCount">
        UPDATE blog
        SET like_count = (
            SELECT COUNT(*)
            FROM like_blog
            WHERE blog_id = #{blogId}
        )
        WHERE id = #{blogId}
    </update>

    <insert id="insertCommentLike">
        INSERT IGNORE INTO like_comment (
            comment_id,
            user_id,
            create_time
        ) VALUES (
            #{commentId},
            #{userId},
            #{createTime}
        )
    </insert>

    <delete id="deleteCommentLike">
        DELETE FROM like_comment
        WHERE comment_id = #{commentId}
          AND user_id = #{userId}
    </delete>

    <update id="refreshCommentLikeCount">
        UPDATE `comment`
        SET like_count = (
            SELECT COUNT(*)
            FROM like_comment
            WHERE comment_id = #{commentId}
        )
        WHERE id = #{commentId}
    </update>
</mapper>
