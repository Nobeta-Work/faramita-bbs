# 2026-08-19 工作日志

## v0.4.1 后台管理（admin）后端实现

**需求来源**：`docs/v0.4/v0.4.1 更新日志.md` 第 1.1/2.1 节，实现后台管理后端接口（Agent MCP 部分此前已实现，本次补齐 admin 模块）。

**技术栈**：Spring Boot 3.5.6 + MyBatis + PageHelper + Spring Security（与项目一致，模块风格参考 `module/agent` 的直接 `@Service` 类风格）。

**新增**（`backend/bbs/src/main/java/cn/nobeta/bbs/module/admin/`，共 36 个 Java 文件）：
- Controller ×6：AdminAuth / AdminRole / AdminPerm / AdminUser / AdminBlog / AdminTag
- Service ×6：AdminAuthService（登录 + ADMIN 角色校验）、AdminRoleService（角色 CRUD + 权限绑定）、AdminPermService、AdminUserService（改状态/角色，封禁清理 Redis 缓存）、AdminBlogService（改状态/删除，删博客清理点赞缓存）、AdminTagService
- Mapper ×1：AdminMapper（resources/mapper/AdminMapper.xml）
- 实体：SysRole、SysPerm；DTO ×16；VO ×5

**接口**：严格按文档 2.1 节 16 个接口实现。

**修改**：SecurityConfig（放行 /api/admin/login，/api/admin/** 需 ADMIN 角色）、ResultCode（+11 错误码）、新增 sql/v0.4.1_init.sql（agent_token 表 + AGENT 角色种子）。

**验证**：mvnw compile 通过。

## v0.4.1 限流 + 多设备登录审查（用户已实现，我审查）

用户实现了：@RateLimit 注解 + RateLimitAspact（AOP）+ rate_limit.lua 令牌桶 + RedisKeys.RATE_LIMIT + Token 缓存改 Hash（jti 隔离多端登录）。

**发现的问题**：
1. 🔴 rate_limit.lua：L14 读 `tokens`，L24 写 `token`（字段名不一致）→ 限流失效
2. 🔴 rate_limit.lua：L25 `EXPIRE KEYS` 应为 `EXPIRE KEYS[1]`（table 传参报错）→ 放行分支抛异常，@RateLimit 接口 500
3. 🔴 AuthServiceImpl.logout 删整个 refresh_token Hash → 登出一个设备踢掉所有设备；需前端登出传 refreshToken 才能 HDEL 单设备 field
4. 🔴 logout L203 判断反了：`if (userId.equals(getUserId(accessToken))) return;` 一致时直接返回 → 登出永不执行（项目原有 bug，应为 `!equals`）
5. 🟡 限流 key `rate_limit:{userId/IP}` 不含接口维度，所有接口共用一个桶
6. 🟡 拒绝分支不 EXPIRE 续期（次要）
7. 🟢 类名拼写 RateLimitAspact → 应为 RateLimitAspect
8. 🟢 login 限流 capacity=5/refill=1 偏松（60 次/分钟）
9. 🟢 refresh 后 login_user 续期 7 天与 access 30 分钟不匹配

**正确的部分**：login/refresh Hash+jti 隔离、JwtAuthenticationFilter 不动（login_user 共享）、admin 封禁删整个 Hash（踢全部设备）语义正确、编译通过。
